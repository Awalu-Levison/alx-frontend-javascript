export default function signUpUser(firstName, lastName) {
  /*Signup user and return data if successful*/
  return promise.resolve({
    firstName: '${firstName}',
    lastName: '${lastname}',
  });
}
